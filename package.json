{
	"type": "module",
	"scripts": {
		"test": "vitest",
		"build:tsc": "tsc -p ./tsconfig.json",
		"build:esbuild": "esbuild src/main.ts --tsconfig=tsconfig.json --outdir=out-esbuild"
	},
	"dependencies": {
		"ts-postgres": "file:export-type.tgz",
		"vitest": "^1.5.0"
	},
	"devDependencies": {
		"@types/node": "20.11.0",
		"esbuild": "0.20.2",
		"typescript": "5.4.5"
	}
}
